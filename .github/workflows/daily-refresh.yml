name: Daily price refresh
on:
  schedule:
    - cron: '17 2 * * *'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  refresh-placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Daily refresh placeholder
        run: echo "Collectors will be connected after their live endpoints are verified."
